#pragma once
#include <cmath>

const double alphaS  = 0.12;
const double ew      = 246.22;
const double el      = 0.332;
const double sW      = sqrt(0.223);
const double gs      = sqrt(4 * M_PI * alphaS);
const double mW      = 80.;
const double mt_pole = 172.5;

const double yt = mt_pole / (ew / sqrt(2.));